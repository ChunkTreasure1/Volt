#pragma once
#include <cstdint>

namespace gem
{
	template<size_t L, typename T = float> struct vec;
	template<size_t C, size_t R, typename T = float> struct mat;
	template<typename T = float> struct qua;
}
